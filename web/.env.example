# Copier ce fichier en .env.local et remplir les valeurs
# cp .env.example .env.local

# Supabase Configuration (côté client)
# Récupérer depuis: Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️  IMPORTANT:
# - Ne JAMAIS commiter de vraies clés !
# - Utiliser uniquement la clé "anon" (publique) ici
# - Ne JAMAIS exposer la clé "service_role" côté client
